# NEXT 100% HEALTHY
1. Remove degraded/offline stubs from agent RPC and wire real VerifyPipeline/PatchPipeline/Ledger dependencies.
2. Fix agent runtime wiring/bootstrap drift so uvicorn factory boots without constructor mismatches.
3. Eliminate renderer/main boot regressions and re-run smoke until clean.
4. Fix TypeScript exactOptionalPropertyTypes/verbatimModuleSyntax/import errors package by package.
5. Restore real health/status/verify/patch/ledger contracts end-to-end.
